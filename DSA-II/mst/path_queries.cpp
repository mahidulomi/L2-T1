#include <bits/stdc++.h>
using namespace std;
class Edge
{
    
}
int main()
{
    int n, m;
    cin >> n >> m;
    // m = number of queries
    for (int i = 0; i < n - 1; i++)
    {
        int u, v, wt;
        cin >> u >> v >> wt;
    }
    vector<int> queries(m);
    for (int i = 0; i < m; i++)
    {
        cin >> queries[i];
    }
}